#include "./drawN.h"
#include <functional>
#include <iostream>
#include <thread>
#include <vector>
using namespace std;

// vector<vector<float>> vectImagen(785, vector<float>(1, 0));

int main() {
  drawGeneral();
  return 0;
}
